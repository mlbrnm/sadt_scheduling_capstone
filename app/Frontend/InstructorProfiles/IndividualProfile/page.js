'use client';
import { useState, useEffect } from "react";
import { supabase } from "../../supabaseClient";

export default function IndivProfile() {
    return (
        <div>
            <h1>Instructor Individual Profile Page</h1>
        </div>
    );
}